#ifndef SUM_VECT_H
#define SUM_VECT_H

#include<vector>
using std::vector;

int Sum1DVector(vector<int> &v);
int Sum2DVector(vector<vector<int>> &v2);
void AddOneToEach(vector<int> &v);

#endif

//Do inclue, using here instead on .cpp file
//all functions must have ; ad neding
//const conflict with reference & ???? 
